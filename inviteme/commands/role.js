module.exports = {
    name: "role",
    data: "lets you create reaction roles",
    format: "prefixrole",
    execute(message, args, bot){
        console.log(args)

       if(args[1] === "create"){
           
       }
    }
}